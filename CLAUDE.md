# CLAUDE.md — 주식 추천 대시보드 운영 지침

이 저장소는 정적 대시보드(GitHub Pages)다. Claude 세션은 아래 규칙을 반드시 따른다.

## 운영 모델
- **시세·지수/종목 기술적 분석·유동성 baseline·관심종목 차트 후보·스냅샷·배포**: `refresh-quotes` GitHub Action이 매일 자동 처리(순수 스크립트, LLM 0). `data/quotes.js`·`data/indices.js`·`data/stock-ta.js`·`data/liquidity-auto.js`·`data/history.js`·`price`·`priceDate`·`upside`·`generatedAt`은 이 Action이 담당 — **직접 조사·수정하지 않는다.** (`data/indices.js`=나스닥·다우·코스피·코스닥, `data/stock-ta.js`=전 종목 — 둘 다 Yahoo 일봉에서 **단기(1–3M: 5·20·60일선·RSI14)·장기(6–12M+: 60·120·200일선·골든크로스)** 로 분리 계산 `scripts/update-indices.js`·`scripts/update-stock-ta.js`, 공유 `scripts/lib-ta.js`; `data/liquidity-auto.js`=금리·일드커브·VIX·HY신용·달러·원달러·코스피 합성 유동성 게이지 baseline `scripts/update-liquidity-gauge.js`; `data/watch-candidates.js`=관심종목 차트 후보(턴어라운드·신고가)와 기존 관심종목 태그 유효성 `scripts/screen-watch.js`.)
- **분석·추천(`data/recommendations.js`)·유동성 판단(`data/liquidity.js`)·기술 대응(`techNote`)·지수 대응(`index-notes.js`)·오늘의 Top Pick(`topPicks`)**: **매일 LA 오전 2시(09:00 UTC) 통합 루틴**이 자동 수행한다(사용자가 채팅으로 "업데이트"를 요청하면 온디맨드로도 갱신). 이 통합 루틴 한 세션에서 ①전 종목 기술(techNote)+밸류(aiTarget) 분석 ②신규 후보 탐색·교체 ③시황·유동성 ④지수 대응 ⑤Top Pick 3 선정 ⑥**관심종목 차트 스크리닝 편입·교체(D절)**까지 끝내고 배포하며, **LA 6am(13:00 UTC) 안전망 루틴**이 신선도를 재점검해 정체 시 복구한다. **유동성은 하이브리드** — 시장지표 baseline(`liquidity-auto.js`)은 매일 Action이 자동 산출하고, `data/liquidity.js`는 거시 이벤트(FOMC·지정학)·내러티브 판단으로 이를 **보정/덮어쓰기**한다. 앱은 `liquidity.js`가 있으면 그것을 우선 표시하고 baseline 을 병기하며, 없으면 baseline 을 게이지로 쓴다.
- **오늘의 Top Pick (`data/recommendations.js`의 top-level `topPicks`)**: 통합 루틴이 **정식 추천(watch 제외)** 중 밸류(상승여력)+단기·장기 기술 신호(매수 이상 우대·급락장 falling knife 회피)+유동성 국면 정합을 종합해 **한국·미국 각각 최선 3종목**을 선정(매일 최신 분석으로 재선정 → 더 나은 종목이 나오면 교체된다). 스키마 `{asOf, note, korea:[{rank,ticker,market,name,reason(한국어 1문장)}×3], us:[…×3]}`. 앱은 **주요 지수 아래 배너**('오늘의 Top Pick')에 **상단 국가 선택 버튼으로 선택된 국가의 Top 3만** 시장 배지·상승여력·현재가→목표가·단기/장기 신호 배지·선정 이유로 표시하고(korea·us 데이터는 둘 다 유지, 표시만 선택 국가), 카드 탭 시 해당 카드로 이동한다. `topPicks.asOf`가 `stock-ta.js` asOf보다 오래되면 '⚠ 미갱신' 표시. `update-reco.js`의 topPicks 패치로 반영(korea·us 그룹), `validate-reco.js`가 국가별 3종목·해당국 정식 추천·한국어 이유·중복을 검증. 큐레이션 데이터가 없으면 앱이 국가별 상위 3으로 폴백.
- **기본(=배포) 브랜치**: `claude/stock-analysis-recommendation-v9310x` (여기 push하면 `deploy-pages`가 자동 배포). 세션에 별도의 지정 작업 브랜치가 있으면 거기에도 같은 커밋을 함께 반영한다(고정 개발 브랜치는 없음 — 세션마다 다르다).

## ★ "업데이트" 요청 처리 프로토콜 (반드시 지킬 것)
사용자가 업데이트를 요청하면 — **버튼(이슈)을 눌렀든 안 눌렀든** — 앱 상단 상태칩이 진행을 표시하도록 **항상 GitHub 이슈로 추적**한다:

1. **이슈 확보**: title에 `업데이트 요청` 이 든 **열린 이슈**를 찾는다(`mcp__github__list_issues`). 있으면 재사용, 없으면 새로 만든다(title: `전체 업데이트 요청 (분석·추천 + 유동성)`).
2. **착수 표시**: 그 이슈에 `🔧 처리 시작` 코멘트를 단다 → 상태칩이 **🔧 처리중**으로 바뀐다.
3. **작업 수행**(아래 내용).
4. **완료 표시**: 결과 요약 코멘트 + 이슈 **닫기**(state=closed) → 상태칩이 **🟢 완료**로 바뀐다.

> 앱 상태칩(`index.html`의 `#updStatus`)은 title에 `업데이트 요청`이 든 **최신 이슈**를 GitHub 공개 API로 읽어 **🟡(열림·코멘트0) → 🔧(열림·코멘트≥1) → 🟢(닫힘)** 로 표시한다. 위 순서를 지켜야 색이 올바르게 흐른다.

## ★★ 온디맨드 업데이트 = 항상 **풀 업데이트** (회전·부분 갱신 금지)
사용자가 채팅으로 "업데이트"를 요청하면 **매번 아래 전 항목을 전량 수행한다.** 평상시 토큰 절약을 위한 **회전 커버리지(롤링 재검증 10종목·tier 1개 그룹·aiTarget 45일 주기)는 온디맨드 요청에서는 적용하지 않는다** — 앱에 들어가는 모든 정보가 그 시점 기준으로 전부 새로 검증돼야 한다. 일부만 하고 "완료"라고 보고하지 말 것.

**풀 업데이트 범위(전 항목 필수)**
1. **backbone 최신화(스크립트, LLM 0)**: `update-quotes.js`·`update-stock-ta.js`·`update-indices.js`·`update-liquidity-gauge.js`·`screen-watch.js` 를 먼저 실행해 시세·기술지표·유동성 baseline·관심 후보를 그 시점 최신으로 만든다(최신 거래일 T 확정).
2. **전 종목 분석(전량)**: `techNote`(단기·장기 대응+sig) · `valueNote` — 예외 없이 전 종목.
3. **전 종목 목표가 재검증(전량)**: `targetPrice` 컨센서스(신뢰 출처 2개 교차확인)·`thesis`·`risks`·`dividendYield`·`earnings` 를 전 종목 재확인(10종목 롤링 아님).
4. **전 그룹 tier 재평가(전량)**: 10개 (주제×국가) 그룹 **전부** 품질 축으로 재랭킹 → `tier`·`tierAsOf`(오늘) 갱신(1개 그룹 회전 아님).
5. **AI 적정가 전량 시도**: `aiTarget` 미산출·`aiAsOf` 경과 종목 **전부** 입력값 검증 재시도(검증 실패분은 생략하되 몇 종목을 왜 못 채웠는지 보고).
6. **신규 후보 탐색**: 10개 (주제×국가) 그룹 전부, 그룹당 최소 2각도.
7. **관심종목 차트 스크리닝**(D절): 이탈 제외·졸업 태그 전환·빈자리 충원(국가별·태그별 5종목).
8. **시황·유동성**: `marketNote`·`marketNoteUS`·`marketNoteKR`, `liquidity.js`(게이지·`drivers`·`headline`·`headlineUS`·`headlineKR`).
9. **지수 대응**: `index-notes.js` 4개 지수 `value`·`short`·`long`·`sigShort`·`sigLong`.
10. **오늘의 Top Pick**: 한국·미국 각 3종목 재선정.
11. **기록·배포**: `snapshot.js`·`WATCHLIST.md` 갱신 → `validate-reco.js` 오류 0 → 커밋·push.

**실행 전략 — 병렬 서브에이전트를 적극 활용한다(필수)**: 독립 조사는 `Task` 로 한 메시지에 동시 fan-out 하고 결과만 구조화 반환받는다(서브에이전트는 파일을 고치지 않는다). 파일 반영은 메인이 **단일 작성자**로 순차 수행해 동시편집 충돌을 막는다. 권장 분할 — ①전 종목 기술+밸류+aiTarget 을 12~16종목 배치로 다수 에이전트 ②목표가 재검증도 배치 분할 ③tier 재평가는 (주제×국가) 그룹당 1에이전트(10개) ④신규 후보 탐색 그룹당 1에이전트(10개) ⑤유동성 미국·한국 각 1 ⑥시황 1. 배치 결과가 오는 대로 순차 반영하고, 중간 커밋으로 진행분을 보존한다.

**완료 보고에 반드시 포함**: 항목별 실제 커버리지 수치(techNote N/전체, valueNote N/전체, 목표가 재검증 N/전체, tier 재평가 그룹 N/10, aiTarget 보유 X/전체·이번 신규 Y·미검증 Z와 그 사유, 후보 탐색 그룹 N/10, 교체 건수). 전량을 못 채운 항목이 있으면 **무엇을 왜 못 했는지 명시**한다(조용한 축소 금지).

## 업데이트 작업 내용
- **공통**: WebSearch만 사용(WebFetch·금융 API 직접 호출은 이 환경에서 403). 수치는 검색 스니펫에 실제로 적힌 값만 쓰고, 확인 못 한 값은 기존값 유지. 완료 후 `node scripts/validate-reco.js` 통과 확인 → 배포 브랜치 커밋·push(경합 시 `git rebase` 후 재푸시).
  - **★경합 충돌 해소 규칙(필수)**: 세션이 작업하는 동안 다른 세션이 `data/recommendations.js` 를 먼저 push 할 수 있다(관심종목 편입 등). rebase 충돌 시 **원격 쪽 변경을 절대 버리지 않는다** — `git checkout --ours/--theirs` 로 한쪽을 통째로 택하거나 자기 작업본으로 덮어쓰는 것은 금지다. 올바른 해소법은 **원격 최신 파일을 기준으로 삼고 자기 변경을 패치로 다시 얹는 것**이다: `git rebase --abort` → `git reset --hard origin/<배포브랜치>` → 이번 세션의 패치 JSON 을 `node scripts/update-reco.js <패치>` 로 재적용 → validate 후 push. 패치 JSON 을 남겨 두지 않았으면 충돌 파일에서 **양쪽 종목을 모두 살리도록** 수동 병합한다. 해소 후 반드시 종목 수와 `theme:"watch"` 종목이 유실되지 않았는지 확인한다.
- **A. 분석·추천 (`data/recommendations.js`)**: `generatedAt`·`marketNote`(오늘 시황·통합)·`marketNoteUS`·`marketNoteKR`(미국·한국 **국가별 시황** — 앱 상단 국가 선택 버튼에 따라 해당 국가 시황만 표시, 없으면 통합 `marketNote` 폴백; 매일 두 국가 모두 갱신) 갱신 → `node scripts/snapshot.js` 기록. `node scripts/performance-report.js`(목표가 소진·성과 부진·**`tierReview` 티어↔성과 정합 점검(참고)**)·`validate-reco.js`(신뢰 출처 부족)로 우선순위 선별. 목표가는 컨센서스(신뢰 출처 2개 이상 교차확인), 논거·리스크·`tier`/`tierAsOf`(**기업 질 기준** 배정 + 신선도 유지 — 아래 '신규 후보 탐색 프로토콜' 5번 '티어 배정' 참조)·dividendYield·earnings 최신화. 지배구조 스크리닝 수행(과거 편출 종목이라도 결격 사유가 해소되고 교체 기준을 충족하면 재편입 가능 — 영구 금지 목록은 없다). (주제×국가) 9종목·tier 3/3/3 유지. **매 업데이트마다 트리거(목표가 소진·성과 부진) 유무와 무관하게 아래 '신규 후보 탐색 프로토콜'을 수행한다.** `node scripts/update-reco.js <패치.json>` 증분 패치로 적용. **시세(price·priceDate·upside)는 건드리지 않는다.**
- **A-2. 기술 대응 (`techNote`, 전 종목·매일 온디맨드)**: 각 종목을 6단계 프레임워크(①추세 ②위치 ③모멘텀 ④거래량 ⑤지지·저항 ⑥변동성/손절)로 **분석은 제대로 하되, 저장·표시는 '단기/장기 대응'만 간결히** 한다. **숫자 backbone 은 매일 자동 갱신되는 `data/stock-ta.js`(RSI·MACD·이동평균 배열·골든크로스·200일선 이격·ADX·스토캐스틱·지지/저항)·`data/quotes.js`(현재가)를 그대로 활용**하고, 스크립트가 못 주는 52주 밴드·거래량 추세만 종목당 WebSearch 로 보완한다. 지표 충돌은 억지 정리·양방향 서술 금지(확률 높은 쪽), 하락추세면 '반등 시도'로만('추세 전환' 금지), 미확인 수치 금지. 스키마(간결): `{asOf, short(단기 대응·핵심 숫자 포함 1~2문장), long(장기 대응·1~2문장), sigShort/sigLong(단기·장기 신호 등급: 적극매도/매도/중립/매수/적극매수 5단계)}` — 형식 예시는 삼성전자(005930). 앱은 단기/장기 대응 앞에 sig 등급을 색상 배지로 표시하고, `techNote.asOf`가 `stock-ta.js` asOf보다 오래되면 카드에 '⚠ 미갱신'을 표시한다. `validate-reco.js` 가 short/long 한국어·asOf 를 검증(오류 0 유지, 10일 경과 경고). `update-reco.js` 의 stocks merge 패치로 반영. 앱은 카드의 '기술 대응' 블록에 단기/장기 두 줄로 표시한다. **기술 대응은 매일 LLM 루틴이 생성한 techNote 만 표시한다(정확도 우선, 토큰0 자동 파이프라인 fallback 없음).** 대신 매일 확실히 갱신되도록 **LA 2am(09:00 UTC) 통합 루틴**은 **자체 검증(생성 후 전 종목 techNote.asOf==오늘 확인, 미달분 재시도)** 하고, 별도 **LA 6am(13:00 UTC) 안전망 루틴**이 techNote·index-notes·topPicks 신선도를 점검해 정체 시 재생성·배포한다 — 하루도 빠지지 않게 이중화. **구식 신호칩(taStrip) 표시는 전면 폐기** — 종목 카드·표·라이트카드 모두 이 '단기/장기 대응'으로 일원화(표는 행 탭 시 상세에서 표시). **종목 밸류 설명 (`valueNote`, 선택·종목별)**: 그 종목의 **밸류에이션 관점**(멀티플·저평가/고평가·상승여력 해석, 1~2문장)을 담는다 — 논거(`thesis`, 사업 강점)·기술(`techNote`, 매매 대응)과 구별해 "왜 싸다/비싸다"에 답한다. aiTarget/aiBasis·상승여력·배당 등 검증된 값만 활용(추정치·미확인 멀티플 금지). 앱은 카드의 '밸류' 줄에 valueNote 가 있으면 그것을 표시하고, 없으면 밸류×타이밍 종합 해석 템플릿으로 폴백한다. `validate-reco.js` 가 한국어·길이를 검증. `update-reco.js` stocks merge 로 반영.
**지수도 동일**: 자동 신호(`data/indices.js`)를 backbone 으로 온디맨드 `data/index-notes.js`(`window.INDEX_NOTES = {asOf, items:{<key>:{value,sigShort,sigLong,short,long}}}`, 키=nasdaq/dow/kospi/kosdaq)에 `value`(각 지수 고유 밸류(멀티플·저PBR·고밸류 등)+유동성 국면을 1~2문장으로)·`short`/`long`(단기·장기 기술 대응)·`sigShort`/`sigLong`(단기·장기 신호 등급 5단계, indices.js 의 short.signal/long.signal 기준)을 남기고 지수 카드에 표시한다(유동성처럼 auto+ondemand 하이브리드). 종목과 동일하게 앱은 단기/장기 앞에 sig 등급 배지를 병기하고, `INDEX_NOTES.asOf`가 `indices.js` asOf보다 오래되면 지수 카드에 '⚠ 미갱신'을 표시한다. 지역 유동성은 미국=nasdaq·dow, 한국=kospi·kosdaq 로 매핑해 `LIQUIDITY_DATA`(없으면 `LIQUIDITY_AUTO`) 게이지 등급을 카드에 병기하며, `value` narrative 는 그 유동성 국면 + 지수 밸류(멀티플·저평가 등)를 함께 읽는다.
- **B. 유동성 (`data/liquidity.js`, 하이브리드의 온디맨드 판단층)**: 시장지표 baseline(`liquidity-auto.js`)은 Action이 매일 자동 산출하므로, 이 온디맨드 갱신은 **baseline 이 못 읽는 것**에 집중한다 — 거시 이벤트(FOMC·금통위 결과·지정학), 느린 거시(M2·연준 대차대조표·Core PCE/CPI·외국인 수급), 내러티브. 미국(연준 금리·FOMC·M2·연준 대차대조표·HY 스프레드·10Y·10Y-2Y 커브·Core PCE/CPI), 한국(한은 금리·금통위·CPI·원달러·외국인 수급) 조사 → 게이지 5단계(`매우 우호`/`우호`/`신중`/`부정`/`매우 부정`)·`asOf`(오늘) 갱신. 등급 변경 시 근거 수치를 `drivers`에 남긴다. baseline 과 등급이 크게 어긋나면 그 이유(이벤트·수급)를 `drivers`에 명시한다. **국가별 요약(`headlineUS`·`headlineKR`, 각 1~2문장) + 통합 `headline` 을 함께 갱신** — 앱은 상단 국가 선택에 따라 해당국 headline 을 표시하고(없으면 통합 `headline` 폴백), 게이지·`drivers` 도 선택 국가만 보여주므로 두 국가 headline 을 매일 모두 최신화한다.
- **C. 관심종목 정밀분석 요청 처리 (`theme:"watch"`)**: 앱의 라이트 카드 '정밀분석 요청' 버튼은 title 이 `관심종목 정밀분석 요청:` 으로 시작하는 GitHub 이슈를 만든다. 온디맨드 세션(또는 사용자가 채팅으로 종목명을 지정)에서 **열린 요청 이슈를 처리**한다: ① `mcp__github__list_issues`(또는 search)로 열린 요청을 찾고, ② 각 종목을 **A와 동일한 기준**(컨센서스 목표가=신뢰 출처 2개 이상 교차확인, 논거·리스크·tier·dividendYield·earnings, 필요시 aiTarget·aiBasis)으로 리서치해 `data/recommendations.js` 의 해당 국가 배열에 `theme:"watch"` 풀카드로 추가한다(에스원 012750 참고). ③ 관심종목은 **9종목·tier 3/3/3 구조 규칙에서 면제**(validate-reco.js 가 `watch` 를 예외 처리)되나, **그 외 종목별 필드 검증·신뢰 출처·목표가 ±50% 가드레일은 동일 적용**된다. ④ 시세·기술분석은 `theme:"watch"` 도 자동 파이프라인(quotes.js·stock-ta.js)이 추천 종목과 똑같이 매일 갱신하므로, 즉시 표시가 필요하면 `node scripts/update-quotes.js`·`node scripts/update-stock-ta.js` 를 한 번 돌려 반영한다. ⑤ `validate-reco.js` 통과 확인 → 커밋·push, 요청 이슈에 요약 코멘트 + 닫기. (편입되면 앱이 localStorage 라이트 카드를 자동으로 숨기고 풀카드로 대체한다 — 렌더가 편성 티커를 중복 제거.)

- **D. 관심종목 차트 스크리닝 편입·교체 (`theme:"watch"` + `watchTag`, 매일 자동)**: 관심 탭은 **차트 패턴 2종**으로 국가별 5종목씩 유지한다 — `watchTag:"턴어라운드"`(바닥 확인 + 5·20·60일선 수렴 + 정배열 완성/근접)와 `watchTag:"신고가"`(물량 소화 후 신고가 갓 돌파). 앱은 관심 카드·표에 이 태그를 색상 배지(턴어라운드=앰버, 신고가=그린)로 표시하며, 관심종목엔 tier 배지를 쓰지 않는다.
  - **차트 탐색은 스크립트가 대신한다(토큰 0)**: `refresh-quotes` Action이 매일 `scripts/screen-watch.js`를 돌려 `data/watch-candidates.js`에 ①`turnaround`·`breakout`(국가별 상위 8, 점수순 — 수렴폭·정배열·바닥 깊이 / 돌파 위치·베이스 길이·타이트함) ②`current`(현재 관심종목의 태그 상태: **유지·졸업·이탈·태그 없음**)를 남긴다. 루틴은 **차트를 다시 조회하지 말고 이 파일만 읽는다.**
  - **루틴이 할 일**: ① `current`에서 **이탈** 종목을 확인해 제외(remove) ② **졸업**(52주 고점 회복) 종목은 `watchTag`를 `신고가`로 전환 검토 ③ 빈자리를 `turnaround`·`breakout` 후보 상위부터 채워 **국가별·태그별 5종목**을 목표로 유지한다.
  - **편입 전 필수 검증 — 스크립트가 못 하는 것**: 후보는 차트만 통과한 상태다. 반드시 WebSearch로 **① 안정적으로 성장 중인지(최근 분기·연간 매출·영업이익/EPS YoY 성장 수치 확인 — 이익 감소·역성장·턴어라운드 초입 기업은 편입 금지)** ② 컨센서스 목표가(신뢰 출처 2개 이상 교차확인) ③ 배당·실적·논거·리스크를 검증해 `theme:"watch"` 풀카드로 `add` 한다(가드레일: 신규 편입엔 서로 다른 신뢰 도메인 2개 필수). 성장 검증에 실패하면 그 후보는 건너뛰고 다음 순위로 넘어간다.
  - **후보가 없으면 억지로 채우지 않는다**: 급락장 등으로 조건 충족 종목이 없으면(예: 2026-07-24 급락 직후 한국 신고가 0건) 그날은 비워 두고 `WATCHLIST.md`에 사유·근접 순위를 남겨 다음 회차에 재시도한다.
  - 편입 후 `node scripts/update-quotes.js`·`node scripts/update-stock-ta.js`를 한 번 돌려 시세·기술 신호를 즉시 채우고, `validate-reco.js` 통과 확인 후 배포한다. 유니버스(안전 우량주 목록)는 `scripts/screen-watch.js`의 `UNIVERSE` 상수에 있으며 종목 추가는 여기에 한다.

## 신규 후보 탐색 프로토콜 (최선 편성 수렴)
목표: 전 그룹 탐색·다각도 검색·워치리스트 승계로, 업데이트가 반복될수록 편성이 "찾을 수 있는 최선"으로 수렴하게 한다.

1. **전 그룹 탐색**: 매 업데이트마다 **10개 (주제×국가) 그룹 전부**를 탐색한다. 순서는 ① 워치리스트 보류 후보 재검증 → ② 목표가 소진·성과 부진·경고 종목이 속한 그룹 → ③ 나머지 그룹(마지막 탐색일이 오래된 순). 탐색을 마친 그룹은 `WATCHLIST.md`의 탐색일을 갱신한다. 세션 한도 등으로 부득이 전부 못 마쳤으면 남은 그룹을 `WATCHLIST.md`에 "미완료"로 명시해 다음 업데이트에서 그 그룹부터 시작한다(조용히 생략 금지).
2. **다각도 검색**: 탐색하는 그룹마다 서로 다른 각도로 **최소 2회** 검색한다 — 예: 컨센서스 상승여력 상위 스크리닝, 최근 실적 서프라이즈·신규 커버리지 개시, 신뢰 매체의 주제별 유망주 리스트, 낙폭 과대+명확한 촉매. 한 각도의 결과만 보고 "후보 없음"으로 결론 내리지 않는다.
3. **워치리스트 승계**: 유망하지만 근거 부족(수치 미확인·출처 1개)으로 편입 보류한 후보는 `WATCHLIST.md`에 사유와 함께 기록하고, **다음 업데이트에서 가장 먼저 재검증**한다. 명확히 열위로 판정해 기각한 후보도 사유와 함께 남겨 중복 조사를 막는다.
4. **교체 판정 기준**(모두 충족 시에만 교체): ① 후보가 주제 정의에 부합, ② 신뢰 출처 2개 이상 교차확인, ③ 목표가 컨센서스·배당수익률이 스니펫에서 확인됨, ④ 기존 종목 대비 명확히 우월(상승여력·실적 모멘텀이 뚜렷이 크거나, 기존 종목에 결격 — 목표가 소진·논거 훼손 — 존재). 하루 최대 10교체, 우월 후보가 없으면 결과에 '교체 0건'을 명시한다(정상 동작).
   - **현재가 미확인 예외**: 최신 현재가만 확인 못 한 경우, ④의 우월성이 가격 불확실성을 감안해도 성립하면(예: 확인된 마지막 가격이 수개월 내 + 우월 마진이 충분) `priceUnverified: true` 플래그와 함께 마지막 확인 가격·날짜로 편입할 수 있다. 앱이 '⚠ 현재가 확인 필요' 마크를 표시하고, quotes.js 가 실제 시세를 확보하면 자동 해제된다. 단, 우월성 판단 자체가 가격에 좌우되는 경우(확인 가격이 너무 오래돼 상승여력을 신뢰할 수 없음)는 편입하지 않는다.
5. **기록**: 업데이트 완료 코멘트에 탐색한 그룹, 검토한 후보와 채택/보류/기각 사유를 남기고 `WATCHLIST.md`를 갱신·커밋한다.
   - **롤링 재검증**(자동 루틴 기준 — **온디맨드 요청 시에는 위 ★★ 절에 따라 전 종목 전량**): 신규 후보 탐색과 별개로, 매 업데이트마다 **기존 편성 중 재검증일이 가장 오래된 최소 10종목**의 목표가 컨센서스·논거 유효성을 재확인하고 `WATCHLIST.md`의 재검증 큐를 갱신한다. 이렇게 하면 전 종목의 논거가 약 9일 주기로 신선하게 유지된다(문제 종목만 손보는 트리거 방식의 사각지대 제거).
   - **티어 배정 = 기업·주식의 질(우수성) 기준**: tier 는 **과거 수익률(성과)이 아니라 그 기업/주식이 얼마나 훌륭한지**로 나눈다. 각 (주제×국가) 그룹 9종목을 아래 품질 축으로 종합 평가해 **가장 우수한 3=Tier1 · 다음 3=Tier2 · 나머지 3=Tier3**(3/3/3 유지). 품질 축(검증된 스니펫 수치만 사용, 미확인 항목은 기존 판단 유지): ① **재무 건전성**(부채비율·현금흐름·이익 안정성) ② **수익성·자본효율**(ROE·영업이익률·마진 추세) ③ **사업 경쟁력·해자**(시장 지위·브랜드·진입장벽·가격결정력) ④ **성장성**(매출·이익 성장 궤적·TAM) ⑤ **주주환원**(배당 지속성·자사주) ⑥ **밸류에이션 매력**(상승여력·저평가 정도, `aiTarget`/`valueNote`) ⑦ **논거 견고성·리스크**(risks 적고 신뢰 출처 탄탄·실적 가시성). **매 업데이트마다** 롤링 재검증 대상 종목의 품질을 재평가해 그룹 내 tier 를 재정렬하며(승격 1건=같은 그룹 강등 1건, `update-reco.js` stocks merge 로 `tier` 갱신, validate 가 3/3/3 강제), 조정한 종목·근거를 완료 코멘트와 `WATCHLIST.md`에 남긴다. **과거 성과는 tier 를 직접 바꾸지 않는다** — `performance-report.js` 의 tier↔성과 점검(`tierReview`)은 품질 판단이 맞는지 되돌아보는 **참고 신호**일 뿐이다(고품질 Tier1이 지속 열위면 품질 근거를 재검토하되 성과만으로 강등하지 않고, 저품질 종목이 잠깐 잘 올랐다고 승격하지 않는다).
   - **티어는 항상 최신 데이터로 유지 — `tierAsOf` 신선도 관리(필수)**: 각 종목의 tier 를 품질 평가한 날을 `tierAsOf`(YYYY-MM-DD)에 기록한다. tier 를 재평가·변경할 때마다 그 종목들의 `tierAsOf`를 **오늘로 갱신**한다. **매 업데이트마다 `tierAsOf`가 가장 오래된 (주제×국가) 그룹을 최신 펀더멘털로 재평가(자동 루틴 기준 — **온디맨드 요청 시에는 10개 그룹 전부 재평가**)**해(그룹 단위 재랭킹 — tier 는 그룹 내 상대순위라 9종목을 함께 봐야 한다) 전 그룹이 회전 커버리지로 신선하게 유지되도록 한다(10개 그룹 → 약 10일 주기). 또한 **구성 종목이 실적을 발표하거나 중대한 펀더멘털 변화(신용등급·대규모 M&A·가이던스 급변)가 생긴 그룹은 회전 순서와 무관하게 즉시 재평가**한다. `validate-reco.js` 가 `tierAsOf` 누락 또는 **14일 경과** 시 경고하므로(watch 면제), 경고 종목의 그룹을 우선 재평가해 경고 0을 지향한다. `update-reco.js` stocks merge 로 `tier`·`tierAsOf` 를 함께 갱신한다.
6. **AI적정가 병기(`aiTarget`·`aiBasis`, 선택)**: 컨센서스 목표가와 별개로, **스니펫에서 검증된 입력값만** 사용해 명시적 공식으로 자체 적정가를 산출·병기할 수 있다 — 이익주는 `검증 EPS × 적정 PER(업종 통상 밴드, 판단 근거 명시)`, 배당주는 `검증 DPS ÷ 요구 배당수익률(밴드·판단 명시)`, 금융주는 PBR-ROE 등. **적정 배수·요구수익률은 가능한 한 검증 가능한 앵커를 인용해 정한다** — ① 그 종목의 역사 평균(선행) PER, ② 증권사 리포트의 Target 배수, ③ 현행 선행 PER(시장 배수) 중 확인된 것을 aiBasis에 인용하고, 앵커와 업종 밴드 중 보수적인 쪽을 택한다(앵커 없이 밴드만으로 정한 경우 그 사실을 명시). 산출일을 `aiAsOf`로 기록하고 45일 경과 시 재산출 대상이다. `aiBasis`에 공식·입력값·판단 근거를 반드시 기록한다(validate 가 근거 없는 aiTarget 저장을 거부, 컨센서스 대비 ±50% 괴리는 경고). 입력값을 검증 못 하면 생략이 정상 — 추정치로 채우지 않는다. **커버리지 목표는 전 종목**: 매 업데이트마다 미산출 종목의 입력값(EPS·DPS) 검증을 재시도하고, 신규 편입 종목은 편입 시점에 함께 산출을 시도한다. **일상 커버리지 유지는 매일 LA 2am 통합 루틴이 담당한다(온디맨드 요청 시에는 미산출·경과 종목 **전량** 재시도)** — 전 종목 techNote 를 생성하는 각 서브에이전트가 담당 배치에서 aiTarget 이 없거나 aiAsOf 45일 경과한 종목의 밸류 입력값을 (기술용 검색과 함께) 검증해 위 공식으로 산출·병기한다. 검증 못 하면 생략하고 다음 실행에서 재시도해 며칠 안에 전 종목 커버리지로 수렴시킨다.

## 가드레일 (코드로 강제됨)
`scripts/update-reco.js`·`scripts/validate-reco.js`가 다음을 강제한다(어기면 저장 거부): 신뢰 출처 2개 이상(커뮤니티·블로그·SNS 금지), 목표가 ±50% 급변 차단, 분석 변경 시 근거 URL 필수, (주제×국가) 9종목·tier 3/3/3 구조.
